{
  "name": "stardy-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply stardy --local",
    "db:migrate:remote": "wrangler d1 migrations apply stardy --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "wrangler": "^4.55.0",
    "typescript": "~6.0.3"
  }
}
